/* eslint-disable react/prop-types */
import styled from "styled-components";

const Container = styled.ul`
  width: 100%;
`;

function MyReviewListView({ user, lessons, reviews }) {
  return <Container></Container>;
}

export default MyReviewListView;
